import {View} from 'react-native';
import React from 'react';

const Gap = ({width, height}: {width: any; height: any}) => {
  return <View style={{width: width, height: height}} />;
};

export default Gap;
